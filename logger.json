{
	"version": 1,
	"disable_existing_loggers": false,
	"formatters": {
		"basic": {
			"format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s",
			"datefmt": "%Y-%m-%d %H:%M:%S"
		}
	},
	"handlers": {
		"console": {
			"class": "logging.StreamHandler",
			"level": "INFO",
			"formatter": "basic",
			"stream": "ext://sys.stdout"
		},
		"file": {
			"class": "logging.FileHandler",
			"level": "INFO",
			"formatter": "basic",
			"filename": "./logs/debug_20240524.log"
		}
	},
	"loggers": {
        "project_name": {
            "level": "DEBUG",
            "handlers": ["console", "file"]
            "propagate": false
        }
    },
    "root": {
        "level": "WARNING",
		"handlers": ["console"]
    }
}
